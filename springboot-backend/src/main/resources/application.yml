spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/your-next-read
    username: your-next-read
    password: your-next-read
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true